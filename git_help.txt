# No início do dia: sincronizar com o remote
git pull origin main

# Trabalhar no código...

# Ao finalizar:
git add .
git commit -m "mensagem descritiva"
git push  # Agora funciona direto, sem precisar de -u






git init 
git add . 
git commit -m "commit 20241118 1734"
git branch -M main 
git remote add origin https://github.com/Professador/ondeEstou.git main
git push -u origin main 

meu github: aclr11




//Para atualizar
git add . 
git commit -m "commit 20241118 1734"
git branch -M main 
git push -u origin main 


# Configuração global (para todos os repositórios)
git config --global user.name "Professador"
git config --global user.email "professador@gmail.com"

git config --global user.email "henrique@users.noreply.github.com"